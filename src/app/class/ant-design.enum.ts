export enum nzStatus {
    wait = 'wait',
    process = 'process',
    finish = 'finish',
    error = 'error'
}

export enum nzSize {
    large = 'large',
    small = 'small',
    default = 'default'
}